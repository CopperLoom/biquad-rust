# Architecture — biquad-fit (Rust)

Parametric EQ optimizer. Computes optimal biquad filter parameters (frequency, gain, Q)
to match a measured frequency response to a target curve. Faithful reimplementation of
[jaakkopasanen/AutoEq](https://github.com/jaakkopasanen/AutoEq)'s pipeline and SLSQP
optimizer in Rust. Zero Python required.

## Reference Implementations

| Source | Role | Local Path |
|--------|------|------------|
| [AutoEQ](https://github.com/jaakkopasanen/AutoEq) (Python) commit `7ae0f56` | **Correctness oracle.** All algorithmic decisions defer to AutoEQ's source. | `../reference-implementations/autoEQ/autoeq/` |
| [biquad-fit](https://github.com/CopperLoom/biquad-fit) v2.1.0 (TypeScript) | **Prior port.** Faithful pipeline, L-BFGS optimizer (diverges from AutoEQ's SLSQP). | `../biquad-fit/src/` |
| [relf/slsqp](https://github.com/relf/slsqp) v1.0.0 (Rust) | **Optimizer dependency.** Native Rust port of the same Fortran SLSQP that scipy wraps. | Cargo dependency |

### Key AutoEQ Source Files

| File | Contents |
|------|----------|
| `autoeq/peq.py` | `PEQ`, `Peaking`, `LowShelf`, `HighShelf` classes. Optimizer, loss function, filter init, convergence callback. |
| `autoeq/frequency_response.py` | `interpolate()`, `center()`, `compensate()`, `_smoothen()`, `equalize()`, `protection_mask()`, `limited_ltr_slope()`, `limited_rtl_slope()`, `find_rtl_start()` |
| `autoeq/utils.py` | `generate_frequencies()`, `smoothing_window_size()`, `log_f_sigmoid()`, `log_log_gradient()` |
| `autoeq/constants.py` | All default values (grid steps, Q ranges, gain ranges, etc.) |
| `peq.yaml` (repo root) | Example PEQ config showing per-parameter locking in practice. See §6 note on shelf overlap. Local: `../reference-implementations/autoEQ/peq.yaml` |

### Key TypeScript Source Files

| File | Contents |
|------|----------|
| `src/optimize.ts` | Full pipeline + L-BFGS optimizer + filter init |
| `src/equalize.ts` | Slope-limiting pipeline (protection mask, RTL start, LTR/RTL limiters) |
| `src/smooth.ts` | Savitzky-Golay filter + two-zone blending |
| `src/biquadResponse.ts` | Biquad coefficient computation + magnitude evaluation |
| `src/interpolate.ts` | Log-spaced grid resampling |
| `src/compensate.ts` | Error computation |
| `src/applyFilters.ts` | Filter cascade application |
| `src/types.ts` | Shared types |
| `docs/joint-optimizer-spec.md` | Detailed algorithm spec for the TypeScript implementation |

---

## Correctness Contract

Given the same inputs (measured FR, target curve, filter constraints), produce outputs
within **0.5 dB RMSE** of AutoEQ's output across all golden-file combinations.

With SLSQP parity, we expect this gap to shrink significantly vs. the TypeScript L-BFGS
implementation. The 0.5 dB threshold is the regression gate — individual combinations
may be much closer.

Golden files: 90 combinations = 5 IEMs x 6 targets x 3 constraint sets.
Generated by running AutoEQ (Python) on the test fixtures.

---

## Pipeline Overview

```
measured FR ──┐
              ├── interpolate (1.01 grid) ── center (1 kHz) ──┐
target FR  ───┘                                                 │
                                                                ├── compensate (error = meas - target)
                                                                │
                                                                v
                                                           equalize()
                                                     ┌─────────┴──────────┐
                                                     │  smooth (2-zone    │
                                                     │    Savitzky-Golay) │
                                                     │  negate            │
                                                     │  slope-limit (LTR  │
                                                     │    + RTL)          │
                                                     │  gain-cap (+6 dB)  │
                                                     │  re-smooth (1/5)   │
                                                     └─────────┬──────────┘
                                                                │
                                                                v
                                               interpolate to optimizer grid (1.02)
                                                                │
                                                                v
                                                     init filters (HSQ->LSQ->PK)
                                                                │
                                                                v
                                                     SLSQP joint optimize
                                                                │
                                                                v
                                                     compute pregain
                                                                │
                                                                v
                                                   { pregain, filters[] }
```

---

## Module Structure

```
src/
├── lib.rs              # Public API: optimize, apply_filters, pipeline functions
├── types.rs            # FilterType, FreqPoint, Filter, FilterSpec, Constraints, OptimizeResult
├── interpolate.rs      # Log-spaced grid resampling (log-linear interpolation)
├── compensate.rs       # error = measured - target (on common grid)
├── smooth.rs           # Savitzky-Golay filter (order 2), two-zone sigmoid blend
├── equalize.rs         # Slope-limited equalization curve
│   ├── protection_mask()
│   ├── find_rtl_start()
│   ├── limited_ltr_slope()
│   └── limited_rtl_slope()
├── optimize.rs         # SLSQP optimizer wrapper + initialization + loss function
│   ├── resolve_specs()
│   ├── init_peaking() / init_low_shelf() / init_high_shelf()
│   ├── joint_loss()          — MSE + sharpness penalty, >10kHz averaging
│   ├── sharpness_penalty()   — PK only
│   ├── encode_params() / decode_params()
│   ├── build_bounds()
│   ├── compute_pregain()
│   └── optimize()            — full pipeline entry point
├── biquad_response.rs  # Biquad coefficient computation + magnitude evaluation
└── apply_filters.rs    # Apply filter cascade + pregain to an FR curve
```

### Dependencies

| Crate | Purpose |
|-------|---------|
| `slsqp` | SLSQP optimizer (replaces custom L-BFGS from TypeScript version) |
| `serde` + `serde_json` | Serialization for golden file tests and API I/O |

No other runtime dependencies.

### Dev Dependencies

| Crate | Purpose |
|-------|---------|
| `approx` | Float comparison in tests |
| `criterion` | Benchmarks |

---

## Algorithm Specification

### 1. Interpolation

Resample FR to a log-spaced grid using log-linear interpolation (linear in log-frequency
space). Binary search for surrounding pair, then lerp.

| Grid | Step | Range | Points |
|------|------|-------|--------|
| Pipeline | 1.01 | 20-20,000 Hz | 695 |
| Optimizer | 1.02 | 20-20,000 Hz | 349 |

Reference: `frequency_response.py:interpolate()` with `pol_order=1` (linear).
TypeScript: `interpolate.ts`.

### 2. Centering

Subtract the dB value at exactly 1 kHz from all points. Applied independently to both
measured and target curves before error computation.

**1 kHz is not on the 1.01 grid** (nearest grid points: ~995 Hz and ~1005 Hz). The dB
value at 1 kHz must be obtained by log-linear interpolation between the two surrounding
grid points — not by indexing the nearest sample. [Gotcha B]

Reference: `frequency_response.py:center(frequency=1000)`.
TypeScript: `optimize.ts:614-619`.

### 3. Compensation (Error)

`compensate()` is a **four-step pipeline**, not a raw subtraction: [Gotcha A]

1. **Interpolate target** onto the measurement grid using the same log-linear spline (k=1).
   Extrapolates linearly beyond the input range — does not clamp to the boundary value. [Gotcha C]
2. **Center target** at 1 kHz (log-linear interpolated, same as §2 — 1 kHz is off-grid). [Gotcha B]
3. **Add `create_target()`** — applies optional tilt, bass boost, and treble boost shelves
   to the centered target. For our golden file inputs (default params) this contributes zero.
4. **Subtract**: `error[i] = measured[i].db - target[i].db`

Reference: `frequency_response.py:compensate()`, `center()`, `create_target()`.
TypeScript: `compensate.ts`.

### 4. Smoothing — Savitzky-Golay, Two-Zone

**Algorithm:** Savitzky-Golay filter, polynomial order 2. This is a convolution with
precomputed least-squares polynomial coefficients.

**Edge handling — `mode='interp'` (critical):** scipy's `savgol_filter` default is
`mode='interp'`, which fits a polynomial to the last `window_length` samples at each
boundary and evaluates it — not mirror, reflect, or nearest. Most Rust savgol crates
default to a different mode. Must use a crate that exposes `mode='interp'` or implement
the edge polynomial fit manually. Incorrect edge mode changes values at the 20 Hz and
20 kHz extremes — exactly where equalize makes slope-limit decisions. [Gotcha J]

**Two independent passes then sigmoid blend:** `_smoothen()` runs savgol with the
normal window over the entire curve, then savgol with the treble window over the entire
curve, producing two full-length arrays. These are sigmoid-blended in the 6–8 kHz
transition zone. It is **not** a single variable-window pass. [Gotcha D]

**Two zones with sigmoid blend:**

| Zone | Window |
|------|--------|
| Below 6 kHz | 1/12 octave |
| Above 8 kHz | 2 octave |
| 6-8 kHz | Sigmoid blend (log-frequency) |

Sigmoid: `k_treble = expit((log10(f) - log10(f_center)) / (half_range / 4))` where
`f_center = sqrt(f_upper / f_lower) * f_lower` (geometric mean of 6000 and 8000).

Window size in samples: `n = round(ln(2^octaves) / ln(avg_step_size))`, rounded to odd,
minimum 3.

Reference: `frequency_response.py:_smoothen()`, `utils.py:smoothing_window_size()`,
`utils.py:log_f_sigmoid()`.
TypeScript: `smooth.ts`.

### 5. Equalization Curve

Full slope-limiting pipeline on the negated smoothed error:

1. Two-zone smooth the error -> `smoothed`
2. Negate: `y = -smoothed` (correction curve)
3. Apply `treble_gain_k`: multiply treble region (above ~8 kHz) by `treble_gain_k` scalar
   **before** clipping to `max_gain`. Reversing the order changes the treble ceiling.
   For our golden files `treble_gain_k=1.0` (no effect), but order must still be correct.
   [Gotcha E, `frequency_response.py:616-621`]
4. Clip to `±max_gain` (default ±6 dB)
5. Find peaks in `y` with prominence >= 1 (positive and negative via `find_peaks(-y)`)
6. If no peaks/dips: `equalization = y`, done
7. Compute protection mask (zones around interior dips lower than neighbors -> limit-free).
   **Early return:** after inserting synthetic endpoint dips, if fewer than 3 total dips,
   return a zero mask immediately (no protection). [Gotcha H]
8. Find RTL start index. **No-dips fallback:** if no dips found, threshold =
   `max(y[0], y[-1])`; RTL start = first index exceeding that threshold. [Gotcha G]
9. Slope-limit LTR from index 0, max_slope = 18 dB/octave, with region validation
   (clipped regions containing no peaks are discarded).
   - Region bounds: `[region_start, i)` — exclusive upper bound. Protection mask is
     checked at index `i`, not `i-1`. Off-by-one here silently drifts the whole LTR
     correction. [Gotcha F]
   - Slope baseline: `limited[-1]` (last already-clipped output value), not `y[i-1]`
     (raw input). Clipping cascades: each clipped point becomes the baseline for the
     next slope calculation. [Gotcha I, `frequency_response.py:738`]
10. Slope-limit RTL from rtl_start (flip, run LTR, flip back)
11. Combine: `min(ltr, rtl)` element-wise
12. Clip positive gain to 6.0 dB (no cap on cuts)
13. Re-smooth with 1/5 octave (both zones use 1/5)

**Note:** `concha_interference` and `max_slope_decay` parameters exist in AutoEQ but
default to `False` and `0.0` respectively. Our golden files do not use them. Omit from
Rust implementation.

Reference: `frequency_response.py:equalize()`, `protection_mask()`, `find_rtl_start()`,
`limited_ltr_slope()`, `limited_rtl_slope()`.
TypeScript: `equalize.ts`.

### 6. Filter Initialization

Filters initialized sequentially against the **remaining target** (equalization curve
minus already-initialized filters' responses).

**Init order** (descending, i.e. first to initialize):
```
HSQ (optimize_fc=true, optimize_q=true)  -> type_ix=2 -> highest
LSQ (optimize_fc=true, optimize_q=true)  -> type_ix=1
PK  (optimize_fc=true, optimize_q=true)  -> type_ix=0 -> lowest (last)
```
Within same type, narrower fc range initialized first.

Filters with any fixed parameters are ordered according to AutoEQ's 12-entry priority
table (see `peq.py:_init_optimizer_params():init_order()`). The full table:

```
 0: Peaking,   optimize_fc=true,  optimize_q=true   -> val = 0*100 + ...
 1: LowShelf,  optimize_fc=true,  optimize_q=true   -> val = 1*100 + ...
 2: HighShelf,  optimize_fc=true,  optimize_q=true   -> val = 2*100 + ...
 3: Peaking,   optimize_fc=true,  optimize_q=false
 4: LowShelf,  optimize_fc=true,  optimize_q=false
 5: HighShelf,  optimize_fc=true,  optimize_q=false
 6: Peaking,   optimize_fc=false, optimize_q=true
 7: LowShelf,  optimize_fc=false, optimize_q=true
 8: HighShelf,  optimize_fc=false, optimize_q=true
 9: Peaking,   optimize_fc=false, optimize_q=false
10: LowShelf,  optimize_fc=false, optimize_q=false
11: HighShelf,  optimize_fc=false, optimize_q=false
```

Sorted descending (highest value first = most constrained initialized first).
Within same priority: `val += 1 / log2(max_fc / min_fc)` if fc is optimized (narrower
range = higher sub-priority).

**Peaking init:** Find biggest peak by `height * width` (scipy `find_peaks` interpolated
widths). Set fc at peak, Q from bandwidth, gain from height.

**LowShelf init:** Find fc where `|mean(target[:ix+1])|` is maximized. Q = clamp(0.7).
Gain = weighted average using **signed** shelf FR as weights:
`gain = dot(target, shelf_fr) / sum(shelf_fr)`.

**HighShelf init:** Find fc where `|mean(target[ix:])|` is maximized. Same Q and gain
logic as LowShelf but using the high shelf response.

**Shelf fc search clamp:** Both LSQ and HSQ init search over `[max(40, min_fc), min(10000, max_fc)]`
regardless of user config — these are hardcoded outer bounds applied before the
user's fc range. (`peq.py:334-335, 386-387`)

**Shelf overlap prevention:** AutoEQ has no built-in algorithm to prevent LSQ and HSQ
filters from overlapping. Instead, overlap is prevented through **per-parameter locking
and constrained fc ranges in the config**. AutoEQ's own `peq.yaml` example demonstrates
this pattern:
- LSQ: `fc: 105` (fixed, not optimized), `q: 0.7` (fixed). Only gain is free.
- HSQ: `min_fc: 5000, max_fc: 12000` (fc optimized but bounded). Q bounded [0.4, 0.7].

The shelf filters can never overlap because the user constrains them via `FilterSpec`,
not because the optimizer enforces separation. This is the primary real-world use case
for per-parameter locking.

Reference: `peq.py:Peaking.init()`, `LowShelf.init()`, `HighShelf.init()`,
`_init_optimizer_params()`. Example config: `peq.yaml` (AutoEQ repo root).
TypeScript: `optimize.ts:initPeaking()`, `initLowShelf()`, `initHighShelf()`.

### 7. SLSQP Optimizer

**Algorithm:** Sequential Least Squares Programming via `relf/slsqp` crate. This is a
native Rust port of the same Fortran SLSQP that scipy's `fmin_slsqp` wraps.

**Parameter encoding** (flat vector, per optimized parameter):
- `fc` -> `log10(fc)` (ensures equal step sizes across octaves)
- `Q` -> linear
- `gain` -> linear

Fixed parameters are **excluded** from the parameter vector and bounds. The loss function
includes their filter response in the cascade but does not perturb them.

**Per-parameter locking:** Each filter has three independent flags:
- `optimize_fc: bool` — if false, fc is fixed at its initial/provided value
- `optimize_q: bool` — if false, Q is fixed
- `optimize_gain: bool` — if false, gain is fixed

When a flag is false, that parameter is not included in the optimization vector or bounds.
The filter still contributes to the cascade response in the loss function. This matches
AutoEQ's `_parse_optimizer_params` / `_init_optimizer_params` / `_init_optimizer_bounds`.

**Bounds** (in encoded space, defaults from AutoEQ constants):

| Type | fc (log10) | Q | gain |
|------|-----------|---|------|
| PK | [log10(20), log10(10000)] | [0.18248, 6.0] | [min_gain, max_gain] |
| LSQ | [log10(20), log10(10000)] | [0.4, 0.7] | [min_gain, max_gain] |
| HSQ | [log10(20), log10(10000)] | [0.4, 0.7] | [min_gain, max_gain] |

User-supplied `fc_range`, `q_range`, `gain_range` override these defaults per filter.

**Loss function:** `sqrt(MSE + sum(sharpness_penalties))`

MSE computed over optimizer grid (1.02 step), range **always** [20, 20000] Hz
(hardcoded, independent of user's `freq_range`).

Above 10 kHz: `_10k_ix = argmin(|f - 10000|)`; slice `[_10k_ix:]` is **inclusive** of
that index. Both target and filter response replaced with their respective averages over
that slice before MSE computation. Only total energy above 10 kHz matters, not
point-to-point spectral accuracy. (`peq.py:449,593`)

**Sharpness penalty** (PK only):
```
gain_limit = -0.09503189270199464 + 20.575128011847003 / Q
x = gain / gain_limit - 1
coeff = 1 / (1 + exp(-100 * x))
penalty = mean((fr * coeff)^2)
```

**Convergence** (callback after each SLSQP iteration):
- Stop if `len(history) > 8 AND population_std(last_8) < min_std`
- Stop if `len(history) > 4 AND population_std(last_4) < min_std / 2`
- Stop at iteration 150 (SLSQP default)
- Best-params restoration: track `best_loss` and `best_x` throughout, return best on stop

`min_std` defaults to 0.002 (`DEFAULT_PEQ_OPTIMIZER_MIN_STD`) but is configurable via
`Constraints.min_std`. Setting it to `None` disables convergence checking entirely,
letting SLSQP run to its 150-iteration limit. AutoEQ's own `peq.yaml` example uses
`min_std: null` for one-off custom optimizations where maximum accuracy is preferred
over speed.

Reference: `peq.py:PEQ.optimize()`, `_optimizer_loss()`, `_callback()`,
`_init_optimizer_params()`, `_init_optimizer_bounds()`, `_parse_optimizer_params()`.
TypeScript: `optimize.ts:jointOptimize()`, `jointLoss()`, `converged()`.

### 8. Pregain

```
max_boost = peq.max_gain  # = max(sum of all filter responses on optimizer grid)
if max_boost > 0: pregain = -(max_boost + PREAMP_HEADROOM)  # PREAMP_HEADROOM = 0.2
else: pregain = 0.0
```

Not clamped. (`peq.py:543-545`, `frequency_response.py:211`)

Reference: `peq.py:PEQ.max_gain`, used in `frequency_response.py:_optimize_peq_filters()`.
TypeScript: `optimize.ts:computePregain()`.

### 9. Biquad Response Evaluation

Coefficient formulas from Audio EQ Cookbook (W3C). Magnitude via phi = 4*sin^2(w/2)
identity (same as AutoEQ):

```
w = 2 * pi * f / fs
phi = 4 * sin(w/2)^2

num = (b0+b1+b2)^2 + (b0*b2*phi - (b1*(b0+b2) + 4*b0*b2)) * phi
den = (a0+a1+a2)^2 + (a0*a2*phi - (a1*(a0+a2) + 4*a0*a2)) * phi
dB = 10 * log10(num) - 10 * log10(den)
```

Where a0 is normalized to 1.0 after coefficient computation. Coefficient formulas for
PK, LSQ, HSQ are in `peq.py:Peaking.biquad_coefficients()`, `LowShelf.biquad_coefficients()`,
`HighShelf.biquad_coefficients()`.

Reference: `peq.py:PEQFilter.fr` property.
TypeScript: `biquadResponse.ts`.

---

## Numerical Constants

| Parameter | Value | AutoEQ Source |
|-----------|-------|---------------|
| Pipeline grid step | 1.01 | `DEFAULT_STEP` |
| Optimizer grid step | 1.02 | `DEFAULT_BIQUAD_OPTIMIZATION_F_STEP` |
| Sample rate default | 44100 | `DEFAULT_FS` |
| Loss freq min | 20 Hz | `DEFAULT_PEQ_OPTIMIZER_MIN_F` (hardcoded) |
| Loss freq max | 20000 Hz | `DEFAULT_PEQ_OPTIMIZER_MAX_F` (hardcoded) |
| 10 kHz averaging cutoff | 10000 Hz | Hardcoded in `_optimizer_loss` |
| Max slope | 18.0 dB/oct | `DEFAULT_MAX_SLOPE` |
| Max positive gain | 6.0 dB | `DEFAULT_MAX_GAIN` |
| Preamp headroom | 0.2 dB | `PREAMP_HEADROOM` |
| Max optimizer iterations | 150 | scipy fmin_slsqp default |
| Convergence MIN_STD | 0.002 (default; configurable, None = disabled) | `DEFAULT_PEQ_OPTIMIZER_MIN_STD` |
| Convergence window | 8 | `_callback` in peq.py |
| PK default Q range | [0.18248, 6.0] | `DEFAULT_PEAKING_FILTER_MIN/MAX_Q` |
| PK default fc range | [20, 10000] | `DEFAULT_PEAKING_FILTER_MIN/MAX_FC` |
| PK default gain range | [-20, 20] | `DEFAULT_PEAKING_FILTER_MIN/MAX_GAIN` |
| Shelf Q range | [0.4, 0.7] | `DEFAULT_SHELF_FILTER_MIN/MAX_Q` |
| Shelf fc range | [20, 10000] | `DEFAULT_SHELF_FILTER_MIN/MAX_FC` |
| Shelf default gain range | [-20, 20] | `DEFAULT_SHELF_FILTER_MIN/MAX_GAIN` |
| Normal smooth window | 1/12 octave | `DEFAULT_SMOOTHING_WINDOW_SIZE` |
| Treble smooth window | 2.0 octaves | `DEFAULT_TREBLE_SMOOTHING_WINDOW_SIZE` |
| Treble transition lower | 6000 Hz | `DEFAULT_TREBLE_SMOOTHING_F_LOWER` |
| Treble transition upper | 8000 Hz | `DEFAULT_TREBLE_SMOOTHING_F_UPPER` |
| Re-smooth window | 1/5 octave | Hardcoded in `equalize()` |

---

## Differences from TypeScript biquad-fit v2.1.0

| Area | TypeScript | Rust | Why |
|------|-----------|------|-----|
| Optimizer | L-BFGS (custom) | SLSQP (`relf/slsqp`) | Match AutoEQ exactly. Eliminates local-minima divergence (e.g. LSQ/HSQ inversions). |
| Per-parameter locking | Not supported | `optimize_fc`/`optimize_q`/`optimize_gain` per filter | Match AutoEQ. Required for fixed-band EQ and eq-coach. |
| Shelf gain init weighting | `abs(shelf_fr)` | `shelf_fr` (signed) | Match AutoEQ: `dot(target, self.fr) / sum(self.fr)`. |
| Smoothing | Savitzky-Golay (order 2) | Same | Both match AutoEQ. |
| Dependencies | Zero | `slsqp`, `serde` | Zero-dep was a JS/browser constraint. Not applicable to Rust crate. |
